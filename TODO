Refaire gestion erreur parcing option -c
Parcing IP


Bonus option    -c envoie x paquet                      X
                -D  affiche l'heure avant la ligne      X
                -w  arrete ping au bout de x secondes   X
                -f  affiche de point pour un paquet     X
                -i  modif intervalle entre 2 envois     X


ft_ping presque fini
bug dans send.c au niveau de recvmsg qui loop a l'infini quand l'ip est mauvaise
PQDN est un peu vague dans mon esprit avec la non utilisation de DNS

dans parcing.c checkip est en com
free_double_char dans error.c ne sert a rien
get_fqdn() dans init_send.c
re faire parcing pour "ping -c5 8.8.8.8" normalement c'est bon et là dans le code c'est mauvais
dans init_send.c dans get_fqdn() test leaks
enleve prinft
changer dans init_send.c dans init_s_addr() la fonction inet_pton() en fonction de ses return
rajoute close(fd)